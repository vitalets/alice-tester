{
  "name": "alice-tester",
  "version": "0.5.2",
  "description": "A module for testing Yandex Alice skills",
  "author": {
    "name": "Vitaliy Potapov",
    "email": "noginsk@rambler.ru"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/vitalets/alice-tester.git"
  },
  "scripts": {
    "code": "eslint src test",
    "test": "mocha test/setup.js test/specs/*.js",
    "test:cov": "nyc --reporter=html npm t && nyc report && nyc check-coverage --lines 100",
    "open:cov": "open coverage/index.html",
    "ci": "run-s code test:cov",
    "coveralls": "nyc report --reporter=text-lcov | coveralls",
    "toc": "markdown-toc README.md -i",
    "prerelease": "run-s code test:cov",
    "postrelease": "git push --follow-tags --no-verify",
    "release": "npm version $VER && npm publish --access public",
    "release-patch": "VER=patch npm run release",
    "release-minor": "VER=minor npm run release"
  },
  "main": "src/index.js",
  "engines": {
    "node": ">=8"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged && npm run test:cov",
      "pre-push": "run-s code test:cov"
    }
  },
  "lint-staged": {
    "{src,test}/**/*.js": "eslint"
  },
  "dependencies": {
    "debug": "^4.1.1",
    "get-value": "^3.0.1",
    "lodash.merge": "^4.6.1",
    "mkdirp": "^0.5.1",
    "node-fetch": "^2.6.0"
  },
  "devDependencies": {
    "assert-rejects": "^1.0.0",
    "chai": "^4.2.0",
    "coveralls": "^3.0.4",
    "eslint": "^6.0.1",
    "eslint-plugin-mocha": "^5.2.0",
    "get-port": "^5.0.0",
    "husky": "^2.5.0",
    "lint-staged": "^8.2.1",
    "markdown-toc": "^1.2.0",
    "mocha": "^6.1.4",
    "nock": "^10.0.6",
    "npm-run-all": "^4.1.5",
    "nyc": "^14.1.1",
    "rimraf": "^2.6.3"
  },
  "license": "MIT"
}
